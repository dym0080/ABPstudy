<div class="portlet light bordered" ng-controller="app.views.softuserlogin as vm">
    <div class="portlet-title">
        <div class="caption">
            <i class="icon icon-grid font-green-sharp"></i>
            <span class="caption-subject font-green-sharp bold uppercase">登录记录</span>
        </div>
    </div>

    <div class="portlet-body" style="padding-top: 0px">

        <!-- BEGIN Portlet PORTLET-->



        <milo-Search search-items="searchItems" search-conds="searchConds" searchHandle="queryData()"></milo-Search>

        <div class="table-scrollable ">
            <table class="table table-bordered table-hover table-striped table-advance">
                <thead>
                    <tr>
                        <th style="width: 40px;text-align: center"> ID </th>
                        <th> 用户 </th>
                        <th style="text-align: left"> 软件 </th>
                        
                        <th style="width:260px;text-align: center"> 机器码 </th>
                        <th style="width: 160px; text-align: center"> ip </th>
                        <th style="width: 160px; text-align: center"> 登录时间 </th>
                    </tr>
                </thead>
                <tbody>

                    <tr role="row" ng-repeat="item in vm.items" class="even">
                        <td style="text-align: center;vertical-align: middle">{{item.id}}</td>
                        <td style="vertical-align: middle">{{item.loginName}}</td>
                        <td style="vertical-align: middle">{{item.softName}}</td>
                        <td style="text-align: center; vertical-align: middle">{{item.mcode}}</td>
                        <td style="text-align: center;vertical-align: middle">{{item.ip}}</td>
                        <td style="text-align: center; vertical-align: middle">{{item.creationTime | date:'yyyy-MM-dd HH:mm:ss'}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <milo-Pagination page-info="pageInfo" pagingHandle="paging()"></milo-Pagination>

    </div>
</div>